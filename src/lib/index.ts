/**
 * Root library entrypoint for the Kavia Design System package.
 *
 * Consumers should import components from the package root:
 * `import { Button } from "kavia-design-system"`
 *
 * Styles are exposed separately via:
 * `import "kavia-design-system/styles.css"`
 */

export * from "../kavia";
